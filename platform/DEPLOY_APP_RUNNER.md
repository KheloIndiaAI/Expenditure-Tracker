# ⚠️ Superseded — App Runner is closed to new customers

As of **30 April 2026, AWS App Runner no longer accepts new customers**, so this
path is not available for the dev/prod accounts.

**Use [`DEPLOY_ECS_EXPRESS.md`](./DEPLOY_ECS_EXPRESS.md) instead** — Amazon ECS
Express Mode is AWS's recommended replacement and reuses the exact same container
image, RDS, secrets, and seed steps. Only the compute layer differs.
